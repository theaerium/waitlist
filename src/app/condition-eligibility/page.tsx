import { createPolicyMetadata, createPolicyPage } from "@/app/legal/policy-page-factory";

export const metadata = createPolicyMetadata("condition-eligibility");

export default createPolicyPage("condition-eligibility");
