import { createPolicyMetadata, createPolicyPage } from "@/app/legal/policy-page-factory";

export const metadata = createPolicyMetadata("anti-money-laundering");

export default createPolicyPage("anti-money-laundering");
