import { createPolicyMetadata, createPolicyPage } from "@/app/legal/policy-page-factory";

export const metadata = createPolicyMetadata("photo-guidelines");

export default createPolicyPage("photo-guidelines");
