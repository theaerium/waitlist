import { createPolicyMetadata, createPolicyPage } from "@/app/legal/policy-page-factory";

export const metadata = createPolicyMetadata("terms-of-service");

export default createPolicyPage("terms-of-service");
